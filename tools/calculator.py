"""
Calculator tool — safe symbolic math via sympy.

Accepts a mathematical expression as a string, parses it with sympy
(never with Python's built-in code execution), and returns a structured
result containing both the numeric answer and the fully simplified form.

Not yet implemented.  Input/output types defined in schemas/tools.py.
"""
