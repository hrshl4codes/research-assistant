# Core agent framework
agno==1.4.4

# LLM via OpenRouter (handled through agno's OpenRouter model class)
openai==1.59.6          # agno's OpenRouter adapter uses the openai client under the hood

# Vector store
duckdb==1.3.0

# Embeddings — local, no API key required
sentence-transformers==3.1.1
torch==2.4.1            # sentence-transformers backend; adjust to torchvision-compatible version for your GPU
numpy==1.26.4

# Document loading
PyMuPDF==1.24.11        # import as `fitz`

# Chunking
langchain-text-splitters==0.3.3

# Structured I/O
pydantic==2.10.4

# Calculator tool — uses sympy for safe symbolic math (no code execution)
sympy==1.13.3

# CLI output
rich==13.9.4

# Environment variable loading
python-dotenv==1.0.1
