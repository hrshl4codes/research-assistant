"""
Agent output schemas — typed responses returned by RetrieverAgent and GeneralAgent.

RetrieverResult carries the answer plus a list of RetrievedChunk objects so
the caller always has access to the raw evidence.  GeneralResult carries the
answer plus any tool call records.

Not yet implemented.
"""
