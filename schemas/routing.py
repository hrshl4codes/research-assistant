"""
RoutingDecision schema — structured output emitted by the Coordinator.

Every routing decision includes an explicit `reasoning` field so the
coordinator's logic is visible to the user, satisfying the evaluation
criterion of auditable routing (not a black-box dispatch).

Not yet implemented.
"""
